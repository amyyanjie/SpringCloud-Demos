server:
  port: 6050
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${register.host}:${server.port}/eureka/
register:
  host: localhost
